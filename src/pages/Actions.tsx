import { useContext } from 'react';
import { ErrorLoggerContext } from '../context/logger-context';

const Actions: React.FC = () => {
  return <div className='container mx-auto flex justify-center'></div>;
};

export default Actions;
